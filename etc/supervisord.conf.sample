; User-specific supervisor configuration

[inet_http_server]          ; inet (TCP) server disabled by default
port=127.0.0.1:20000        ; (ip_address:port specifier, *:port for all iface)
username=admin              ; (default is no username (open server))
password=sparcs             ; (default is no password (open server))

;[program:arara_engine]
;autostart=false            ; uncomment if you don't want to use arara_engine
